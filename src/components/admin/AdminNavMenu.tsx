// AdminNavMenu is superseded by the persistent AdminShell sidebar.
// Kept as a no-op export so existing page headers can still import it
// without rendering the old drawer trigger.
export function AdminNavMenu() {
  return null;
}
